---
import Page from '../../layouts/Page.astro';
import { SITE_SEO_TITLE } from '../../consts';

// This page is a PREVIEW + link for beijerterm.com, not a search box: a
// screenshot of the real tool, a short blurb, and a button. It is its own
// page with its own (self-referencing) canonical — authority flows to
// beijerterm.com through the ordinary <a> links below, never via canonical.
const BEIJERTERM = 'https://beijerterm.com';
---

<Page
	title={`Beijerterm | ${SITE_SEO_TITLE}`}
	description="Beijerterm — my curated multilingual terminology database for technical translators. Search it free at beijerterm.com, or inside Supervertaler."
>
	<section class="bt">
		<h1>Beijerterm</h1>
		<p class="bt-lede">
			My curated, searchable multilingual terminology database for technical
			translators — Dutch&nbsp;↔&nbsp;English and more, with full provenance for
			every source. Free to search, and built into Supervertaler.
		</p>

		<p class="bt-cta">
			<a class="bt-btn" href={`${BEIJERTERM}/`}>Open Beijerterm ↗</a>
		</p>

		<a class="bt-shot" href={`${BEIJERTERM}/`} aria-label="Open Beijerterm">
			<img
				src="/beijerterm-preview.png?v=1"
				width="1369"
				height="1109"
				alt="The Beijerterm terminology search at beijerterm.com – a Dutch-to-English search for ‘trein’ returning matching terms, each with a source badge, above a Sources tab listing every glossary behind the database."
			/>
		</a>

		<p class="bt-foot">
			Search it at <a href={`${BEIJERTERM}/`}>beijerterm.com</a>, or one keystroke
			away as a SuperLookup resource inside Supervertaler Workbench and Supervertaler
			for Trados.
		</p>
	</section>
</Page>

<style>
	.bt { margin-bottom: 2.5em; }
	.bt h1 { margin-bottom: 0.2em; }
	.bt-lede {
		color: rgb(var(--muted));
		margin-top: 0;
		line-height: 1.65;
		max-width: 42em;
	}
	.bt-cta { margin: 1.4em 0; }
	.bt-btn {
		display: inline-block;
		font-weight: 600;
		font-size: 1.05em;
		padding: 0.6em 1.3em;
		border-radius: 8px;
		background: rgb(var(--text));
		color: rgb(var(--bg));
		text-decoration: none;
		border: 1px solid rgb(var(--text));
	}
	.bt-btn:hover { opacity: 0.85; }
	.bt-shot {
		display: block;
		border-radius: 10px;
	}
	.bt-shot img {
		display: block;
		width: 100%;
		height: auto;
		border: 1px solid rgb(var(--border));
		border-radius: 10px;
		box-shadow: 0 2px 16px rgba(0, 0, 0, 0.08);
	}
	.bt-foot {
		margin-top: 1.4em;
		color: rgb(var(--muted));
		font-size: 0.92em;
		line-height: 1.6;
		max-width: 42em;
	}
</style>
